optimal solution

class Solution {
    public int largestElement(int[] nums) {
    
    int highest = nums[0];

    for(int i = 0; i < nums.length; i++){
        if(nums[i] > highest){
            highest = nums[i];
        }
    }
     return highest;
    }
}
